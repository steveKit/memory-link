<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for MemoryLink.
    
    Security features:
    - HTTPS enforced for all connections
    - Certificate pinning for Google APIs
    - Clear-text traffic blocked
    
    Per .clinerules/20-android.md: Enforce HTTPS and certificate pinning.
-->
<network-security-config>
    <!-- Block clear-text (HTTP) traffic globally -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Google APIs domain configuration -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">google.com</domain>
        <domain includeSubdomains="true">gstatic.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Debug configuration: Allow localhost for testing -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
